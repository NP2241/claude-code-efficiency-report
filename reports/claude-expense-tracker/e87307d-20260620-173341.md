# Claude Code Efficiency Report

**Revert "refactor: replace JSON storage with SQLite backend (expenses.db)"**

Generated: 2026-06-20T17:33:41.567370+00:00

## Executive summary

**Revert "refactor: replace JSON storage with SQLite backend (expenses.db)"** — High efficiency — $0.00 · 0 tokens · -21 net lines

**Verdict:** High efficiency — $0.00 · 0 tokens · -21 net lines


> No token usage in commit time window — commit may predate Claude work or cwd did not match repo.


## Usage & cost

| Metric | Value |
|--------|-------|
| Total tokens | 0 |
| Estimated cost | $0.0000 |
| Sessions | 0 |
| API turns | 0 |
| Data source | transcripts |

### Model breakdown


No per-model breakdown available.


### Model switches


No model switches in this commit window.


## Work completed

.gitignore, expense-cli/README.md, expense-cli/expense_cli/cli.py, expense-cli/expense_cli/models.py, expense-cli/expense_cli/storage.py, expense-cli/tests/test_expense_cli.py

## Git evidence

- **SHA:** `e87307d0a44e957cb4c5065fbd26c2315fe015e9`
- **Author:** Neil Pendyala
- **Committed:** 2026-06-20T10:21:29-07:00
- **Window:** 2026-06-20T17:21:29+00:00 → 2026-06-20T17:21:29+00:00

```
.gitignore                            |   6 +-
 expense-cli/README.md                 |  31 ++---
 expense-cli/expense_cli/cli.py        |  17 ++-
 expense-cli/expense_cli/models.py     |  17 ++-
 expense-cli/expense_cli/storage.py    |  85 ++++--------
 expense-cli/tests/test_expense_cli.py | 253 +++++++++++++++++-----------------
 6 files changed, 194 insertions(+), 215 deletions(-)
```

## Rework analysis



- **REVERT** (high): Commit reverts prior work — likely wasted AI spend. — _Subject: Revert "refactor: replace JSON storage with SQLite backend (expenses.db)"_

- **ADD_DELETE_SPIKE** (medium): Large add+delete spike on expense-cli/tests/test_expense_cli.py. — _+129/-124 lines_



## Code quality

Tests touched: expense-cli/tests/test_expense_cli.py · Diff size: +194/-215 (6 files)

## Efficiency score

**High**

0 tokens (mostly unknown) over 0 session(s) produced -21 net lines across 6 file(s). 2 rework flag(s) (ADD_DELETE_SPIKE, REVERT). Efficiency: **High**.



## Budget recommendation

Continue current workflow — $0.00 for this commit is reasonable for -21 net lines.

## Evidence appendix

Session IDs: none

<details>
<summary>Sample diff (truncated)</summary>

```
diff --git a/.gitignore b/.gitignore
index 9672158..d0834ba 100644
--- a/.gitignore
+++ b/.gitignore
@@ -1,7 +1,5 @@
-# runtime data files (generated on first use)
-expenses.db
-expenses.db-wal
-expenses.db-shm
+# expenses data file (generated at runtime)
+expenses.json
 
 # virtual environments
 .venv/
diff --git a/expense-cli/README.md b/expense-cli/README.md
index 2aa1e5b..66b0be5 100644
--- a/expense-cli/README.md
+++ b/expense-cli/README.md
@@ -117,23 +117,16 @@ python -m expense_cli import-csv /path/to/backup.csv
 
 ## Data file
 
-Expenses are stored in an SQLite database (`expenses.db`) in the current
-working directory.  The file is created automatically on first use.
-
-Schema:
-
-```sql
-CREATE TABLE expenses (
-    id       INTEGER PRIMARY KEY AUTOINCREMENT,
-    date     TEXT    NOT NULL,
-    amount   REAL    NOT NULL,
-    category TEXT    NOT NULL,
-    note     TEXT    NOT NULL
-);
-```
-
-You can inspect the database directly with the `sqlite3` CLI:
-
-```bash
-sqlite3 expenses.db "SELECT * FROM expenses;"
+Expenses are saved to `expenses.json` in the current working directory:
+
+```json
+[
+  {
+    "id": 1,
+    "date": "2026-06-20",
+    "amount": 12.5,
+    "category": "food",
+    "note": "Lunch at the deli"
+  }
+]
 ```
diff --git a/expense-cli/expense_cli/cli.py b/expense-cli/expense_cli/cli.py
index 1a62835..d29e6be 100644
--- a/expense-cli/expense_cli/cli.py
+++ b/expense-cli/expense_cli/cli.py
@@ -18,13 +18,15 @@ CSV_FIELDS = ["id", "date", "amount", "category", "note"]
 
 def cmd_add(args):
     """Append a new expense entry."""
+    expenses = storage.load()
     try:
-        entry = create_expense(args.amount, args.category, args.note)
+        entry = create_expense(args.amount, args.category, args.note, expenses)
     except ValueError as exc:
         sys.exit(f"Error: {exc}")
-    assigned_id = storage.add(entry)
+    expenses.append(entry)
+    storage.save(expenses)
     print(
-        f"Added expense #{assigned_id}: "
+        f"Added expense #{entry['id']}: "
         f"${entry['amount']:.2f} [{entry['category']}] - {entry['note']}"
     )
 
@@ -150,7 +152,14 @@ def cmd_import_csv(args):
         print("No expenses to import.")
         return
 
-    storage.add_many(rows)
+    expenses = storage.load()
+    next_id = (expenses[-1]["id"] + 1) if expenses else 1
+    for row in rows:
+        row["id"] = next_id
+        next_id += 1
+        expenses.append(row)
+
+    storage.save(expenses)
     print(f"Imported {len(rows)} expense(s).")
 
 
diff --git a/expense-cli/expense_cli/models.py b/expense-cli/expense_cli/models.py
index df061e7..64e74ec 100644
--- a/expense-cli/expense_cli/models.py
+++ b/expense-cli/expense_cli/models.py
@@ -3,21 +3,20 @@
 from datetime import date
 
 
-def create_expense(amount: float, category: str, note: str) -> dict:
+def create_expense(amount: float, category: str, note: str,
+                   existing: list) -> dict:
     """Build a new expense dict ready for storage.
 
-    Handles date stamping, cent rounding, and whitespace stripping.
-    Raises ``ValueError`` if *amount* is not positive.
-
-    The ``id`` field is intentionally absent — the storage layer assigns
-    it via SQLite AUTOINCREMENT on insert.
+    Handles id generation, date stamping, cent rounding, and whitespace
+    stripping.  Raises ``ValueError`` if *amount* is not positive.
     """
     if amount <= 0:
         raise ValueError("amount must be a positive number")
 
     return {
-        "date":     str(date.today()),
-        "amount":   round(amount, 2),
+        "id": (existing[-1]["id"] + 1) if existing else 1,
+        "date": str(date.today()),
+        "amount": round(amount, 2),
         "category": category.strip(),
-        "note":     note.strip(),
+        "note": note.strip(),
     }
diff --git a/expense-cli/expense_cli/storage.py b/expense-cli/expense_cli/storage.py
index 977f73a..eb6e7de 100644
--- a/expense-cli/expense_cli/storage.py
+++ b/expense-cli/expense
```

</details>